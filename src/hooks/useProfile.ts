// This hook has been removed. Profile data is now fetched server-side
// in NavbarWrapper (src/components/layout/NavbarWrapper.tsx) and passed
// as a prop to client components. This follows the backend-first rule:
// frontend components must not query the database directly.
export {};
